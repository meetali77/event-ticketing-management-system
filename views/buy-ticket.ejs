<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Purchase tickets for events." />
    <link rel="stylesheet" href="index.css" />

    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #f8f9fa;
      }
      h1 {
        text-align: center;
        color: #333;
      }
      main {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        background-color: #fff;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
      }
      form {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
      input,
      button {
        padding: 10px;
        font-size: 1rem;
      }
      .buy-button {
        background-color: #007bff;
        color: white;
        border: none;
        cursor: pointer;
        border-radius: 5px;
      }
      .buy-button:hover {
        background-color: #0056b3;
      }
    </style>
  </head>
  <body class="bg-gray-100 font-sans">
    <h1 class="text-3xl text-center text-gray-800 mt-8">
      Buy Event Tickets for: <%=event.name %>
    </h1>
    <p class="text-center text-gray-700">
      Event Description: <br />
      <%= event.description %>
    </p>
    <p class="text-center text-gray-700">
      Price per Ticket: <%= event.tickets_price %>
    </p>
    <p class="text-center text-gray-700">
      Tickets Available: <%= event.tickets_available %>
    </p>

    <form
      action="/purchase/<%= event.id %>"
      method="POST"
      <label for="name">Your Name:</label>
      <input type="text" name="name" required 
      class="max-w-lg mx-auto p-6 bg-white rounded-lg shadow-md mt-6"
    >
      <label for="quantity" class="text-lg text-gray-800">Quantity:</label>
      <input
        type="number"
        name="quantity"
        min="1"
        max="<%=event.tickets_available %>"
        required
        class="mt-2 p-3 border border-gray-300 rounded w-full focus:outline-none focus:ring-2 focus:ring-blue-500"
      />
      <button
        type="submit"
        class="mt-4 py-3 bg-blue-500 text-white w-full rounded-lg hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500"
      >
        Purchase Tickets
      </button>
    </form>

    <!-- Back Button -->
    <form action="/attendee" method="GET" class="text-center mt-4">
      <button
        type="submit"
        class="px-4 py-2 bg-gray-500 text-white rounded-lg hover:bg-gray-600 focus:outline-none focus:ring-2 focus:ring-gray-500"
      >
        Back to Events
      </button>
    </form>
  </body>
</html>
